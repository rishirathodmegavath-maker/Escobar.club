# Copy this file to .env (or .env.local) before running `npm run dev`.
# Vite only exposes browser env vars prefixed with VITE_, and reads them from this
# directory (frontend/), not the repo-root .env used by docker-compose.

# Same Google OAuth Client ID as the root .env's GOOGLE_CLIENT_ID.
VITE_GOOGLE_CLIENT_ID=
